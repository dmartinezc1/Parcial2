import { useEffect } from "react"
import { useParams } from "react-router-dom"

function Detail(){

    useEffect(()=>{
        const id = useParams()
        fetch('http://localhost:4000/books/'+id).then(_=>console.log(_))
    },[])
    return (
        <div>
            
        </div>
    )
}export default Detail